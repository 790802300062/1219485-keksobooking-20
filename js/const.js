'use strict';

(function () {

  var MapSize = {
    MIN_X: 0,
    MAX_X: 1200,
    MIN_Y: 130,
    MAX_Y: 630
  };
  var MOUSE_LEFT_BUTTON = 1;

  var Key = {
    ENTER: 'Enter',
    ESCAPE: 'Escape'
  };

  window.const = {
    MapSize: MapSize,
    MOUSE_LEFT_BUTTON: MOUSE_LEFT_BUTTON,
    Key: Key
  };
})();
